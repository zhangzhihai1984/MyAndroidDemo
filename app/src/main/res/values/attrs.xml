<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="Themes">
        <attr name="waveViewStyle" format="reference" />
    </declare-styleable>

    <!-- WaveView -->
    <declare-styleable name="WaveView">
        <attr name="wave_above_color" format="color" />
        <attr name="wave_below_color" format="color" />
        <attr name="progress" format="integer" />
        <attr name="wave_length_multiple" format="float" />
        <attr name="wave_height" format="dimension" />
        <attr name="wave_hz" format="float" />
    </declare-styleable>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <attr name="page_background" format="color" />
    <attr name="icon_fill_light" format="color" />

</resources>